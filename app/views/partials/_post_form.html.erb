<%= form_with model: @post do |f| %>
  <div>
    <%= f.label :title %>
    <%= f.text_area :title %>
  </div>
  <br />
  <div>
    <%= f.label :body%>
    <%= f.rich_text_area :body %>
  </div>
  <div>
    <%= f.submit "Save" %>
  </div>
<% end %>
